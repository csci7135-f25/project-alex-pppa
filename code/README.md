# project/code

Install Rust https://rust-lang.org/tools/install/

Navigate to the `rc_and_refcell_analyzer` folder. Run `cargo run` to run the program. Modify the code in `fn main` to change what program gets run.
